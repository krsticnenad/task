/**
 * Query keys for roles-lreated React Query
 *
 * Provides type-safe identifiers for caching.
 */
export const RolesKeys = {
  /**
   * Key for fetching all roles.
   */
  all: ["roles"] as const,
};
